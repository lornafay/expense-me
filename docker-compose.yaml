version: "3"
services:
  frontend:
    build:
      context: app/.
    ports:
        - "3001:3000"
    container_name: react-container
    volumes:
      - react_build:/react/build
    stdin_open: true
    restart: always


volumes:
  react_build: